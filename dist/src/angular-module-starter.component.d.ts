import { AngularModuleStarterService } from './angular-module-starter.service';
export declare class AngularModuleStarterComponent {
    private service;
    message: string;
    constructor(service: AngularModuleStarterService);
}
